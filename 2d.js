function wypiszTablice(tablica) {
    for (let i = 0; i < tablica.length; i++) {
        console.log(tablica[i]);
    }
}

const liczby = [1, 2, 3, 4, 5];
console.log("Wypisanie zawartości tablicy:");
wypiszTablice(liczby);
